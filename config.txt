# supports comments and multiple ranges!
# format:
#    param : value
# or
#    param : from - to

# options are blocking, nonblocking, async
server : blocking

n : 100 - 200
m : 20
delta : 1000
x : 1
